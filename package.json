{
  "name": "gatepass",
  "private": true,
  "version": "1.0.0",
  "description": "GatePass — NIM-native event ticketing Mini App for Nimiq Pay",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @gatepass/api & npm run dev -w @gatepass/web",
    "dev:web": "npm run dev -w @gatepass/web",
    "dev:api": "npm run dev -w @gatepass/api",
    "build": "npm run build -w @gatepass/shared && npm run build -w @gatepass/api && npm run build -w @gatepass/web",
    "test": "npm run test -w @gatepass/shared",
    "start:api": "npm run start -w @gatepass/api"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
